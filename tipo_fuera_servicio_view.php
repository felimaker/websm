<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/tipo_fuera_servicio.php");
	include_once("{$currDir}/tipo_fuera_servicio_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('tipo_fuera_servicio');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'tipo_fuera_servicio';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`tipo_fuera_servicio`.`id`" => "id",
		"`tipo_fuera_servicio`.`tipo_fuera_servicio`" => "tipo_fuera_servicio",
		"`tipo_fuera_servicio`.`descripcion`" => "descripcion",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`tipo_fuera_servicio`.`id`',
		2 => 2,
		3 => 3,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`tipo_fuera_servicio`.`id`" => "id",
		"`tipo_fuera_servicio`.`tipo_fuera_servicio`" => "tipo_fuera_servicio",
		"`tipo_fuera_servicio`.`descripcion`" => "descripcion",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`tipo_fuera_servicio`.`id`" => "ID",
		"`tipo_fuera_servicio`.`tipo_fuera_servicio`" => "Tipo fuera servicio",
		"`tipo_fuera_servicio`.`descripcion`" => "Descripcion",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`tipo_fuera_servicio`.`id`" => "id",
		"`tipo_fuera_servicio`.`tipo_fuera_servicio`" => "tipo_fuera_servicio",
		"`tipo_fuera_servicio`.`descripcion`" => "descripcion",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`tipo_fuera_servicio` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'tipo_fuera_servicio_view.php';
	$x->RedirectAfterInsert = 'tipo_fuera_servicio_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Tipo fuera servicio';
	$x->TableIcon = 'resources/table_icons/cog_add.png';
	$x->PrimaryKey = '`tipo_fuera_servicio`.`id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Tipo fuera servicio', 'Descripcion', ];
	$x->ColFieldName = ['tipo_fuera_servicio', 'descripcion', ];
	$x->ColNumber  = [2, 3, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/tipo_fuera_servicio_templateTV.html';
	$x->SelectedTemplate = 'templates/tipo_fuera_servicio_templateTVS.html';
	$x->TemplateDV = 'templates/tipo_fuera_servicio_templateDV.html';
	$x->TemplateDVP = 'templates/tipo_fuera_servicio_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: tipo_fuera_servicio_init
	$render = true;
	if(function_exists('tipo_fuera_servicio_init')) {
		$args = [];
		$render = tipo_fuera_servicio_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: tipo_fuera_servicio_header
	$headerCode = '';
	if(function_exists('tipo_fuera_servicio_header')) {
		$args = [];
		$headerCode = tipo_fuera_servicio_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: tipo_fuera_servicio_footer
	$footerCode = '';
	if(function_exists('tipo_fuera_servicio_footer')) {
		$args = [];
		$footerCode = tipo_fuera_servicio_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
