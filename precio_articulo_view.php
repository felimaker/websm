<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/precio_articulo.php");
	include_once("{$currDir}/precio_articulo_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('precio_articulo');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'precio_articulo';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`precio_articulo`.`id`" => "id",
		"`precio_articulo`.`articulo`" => "articulo",
		"`precio_articulo`.`precio`" => "precio",
		"`precio_articulo`.`proveedor`" => "proveedor",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`precio_articulo`.`id`',
		2 => 2,
		3 => 3,
		4 => 4,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`precio_articulo`.`id`" => "id",
		"`precio_articulo`.`articulo`" => "articulo",
		"`precio_articulo`.`precio`" => "precio",
		"`precio_articulo`.`proveedor`" => "proveedor",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`precio_articulo`.`id`" => "ID",
		"`precio_articulo`.`articulo`" => "Articulo",
		"`precio_articulo`.`precio`" => "Precio",
		"`precio_articulo`.`proveedor`" => "Proveedor",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`precio_articulo`.`id`" => "id",
		"`precio_articulo`.`articulo`" => "articulo",
		"`precio_articulo`.`precio`" => "precio",
		"`precio_articulo`.`proveedor`" => "proveedor",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`precio_articulo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'precio_articulo_view.php';
	$x->RedirectAfterInsert = 'precio_articulo_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Precio articulo';
	$x->TableIcon = 'resources/table_icons/coins_add.png';
	$x->PrimaryKey = '`precio_articulo`.`id`';

	$x->ColWidth = [150, 150, 150, ];
	$x->ColCaption = ['Articulo', 'Precio', 'Proveedor', ];
	$x->ColFieldName = ['articulo', 'precio', 'proveedor', ];
	$x->ColNumber  = [2, 3, 4, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/precio_articulo_templateTV.html';
	$x->SelectedTemplate = 'templates/precio_articulo_templateTVS.html';
	$x->TemplateDV = 'templates/precio_articulo_templateDV.html';
	$x->TemplateDVP = 'templates/precio_articulo_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: precio_articulo_init
	$render = true;
	if(function_exists('precio_articulo_init')) {
		$args = [];
		$render = precio_articulo_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: precio_articulo_header
	$headerCode = '';
	if(function_exists('precio_articulo_header')) {
		$args = [];
		$headerCode = precio_articulo_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: precio_articulo_footer
	$footerCode = '';
	if(function_exists('precio_articulo_footer')) {
		$args = [];
		$footerCode = precio_articulo_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
