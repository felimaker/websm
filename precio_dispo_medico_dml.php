<?php

// Data functions (insert, update, delete, form) for table precio_dispo_medico

// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

function precio_dispo_medico_insert(&$error_message = '') {
	global $Translation;

	// mm: can member insert record?
	$arrPerm = getTablePermissions('precio_dispo_medico');
	if(!$arrPerm['insert']) return false;

	$data = [
		'dispo_medico' => Request::lookup('dispo_medico', ''),
		'precio' => Request::val('precio', ''),
		'proveedor' => Request::val('proveedor', ''),
	];


	// hook: precio_dispo_medico_before_insert
	if(function_exists('precio_dispo_medico_before_insert')) {
		$args = [];
		if(!precio_dispo_medico_before_insert($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$error = '';
	// set empty fields to NULL
	$data = array_map(function($v) { return ($v === '' ? NULL : $v); }, $data);
	insert('precio_dispo_medico', backtick_keys_once($data), $error);
	if($error)
		die("{$error}<br><a href=\"#\" onclick=\"history.go(-1);\">{$Translation['< back']}</a>");

	$recID = db_insert_id(db_link());

	update_calc_fields('precio_dispo_medico', $recID, calculated_fields()['precio_dispo_medico']);

	// hook: precio_dispo_medico_after_insert
	if(function_exists('precio_dispo_medico_after_insert')) {
		$res = sql("SELECT * FROM `precio_dispo_medico` WHERE `id`='" . makeSafe($recID, false) . "' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)) {
			$data = array_map('makeSafe', $row);
		}
		$data['selectedID'] = makeSafe($recID, false);
		$args=[];
		if(!precio_dispo_medico_after_insert($data, getMemberInfo(), $args)) { return $recID; }
	}

	// mm: save ownership data
	set_record_owner('precio_dispo_medico', $recID, getLoggedMemberID());

	// if this record is a copy of another record, copy children if applicable
	if(!empty($_REQUEST['SelectedID'])) precio_dispo_medico_copy_children($recID, $_REQUEST['SelectedID']);

	return $recID;
}

function precio_dispo_medico_copy_children($destination_id, $source_id) {
	global $Translation;
	$requests = []; // array of curl handlers for launching insert requests
	$eo = ['silentErrors' => true];
	$safe_sid = makeSafe($source_id);

	// launch requests, asynchronously
	curl_batch($requests);
}

function precio_dispo_medico_delete($selected_id, $AllowDeleteOfParents = false, $skipChecks = false) {
	// insure referential integrity ...
	global $Translation;
	$selected_id = makeSafe($selected_id);

	// mm: can member delete record?
	if(!check_record_permission('precio_dispo_medico', $selected_id, 'delete')) {
		return $Translation['You don\'t have enough permissions to delete this record'];
	}

	// hook: precio_dispo_medico_before_delete
	if(function_exists('precio_dispo_medico_before_delete')) {
		$args = [];
		if(!precio_dispo_medico_before_delete($selected_id, $skipChecks, getMemberInfo(), $args))
			return $Translation['Couldn\'t delete this record'] . (
				!empty($args['error_message']) ?
					'<div class="text-bold">' . strip_tags($args['error_message']) . '</div>'
					: '' 
			);
	}

	sql("DELETE FROM `precio_dispo_medico` WHERE `id`='{$selected_id}'", $eo);

	// hook: precio_dispo_medico_after_delete
	if(function_exists('precio_dispo_medico_after_delete')) {
		$args = [];
		precio_dispo_medico_after_delete($selected_id, getMemberInfo(), $args);
	}

	// mm: delete ownership data
	sql("DELETE FROM `membership_userrecords` WHERE `tableName`='precio_dispo_medico' AND `pkValue`='{$selected_id}'", $eo);
}

function precio_dispo_medico_update(&$selected_id, &$error_message = '') {
	global $Translation;

	// mm: can member edit record?
	if(!check_record_permission('precio_dispo_medico', $selected_id, 'edit')) return false;

	$data = [
		'dispo_medico' => Request::lookup('dispo_medico', ''),
		'precio' => Request::val('precio', ''),
		'proveedor' => Request::val('proveedor', ''),
	];

	// get existing values
	$old_data = getRecord('precio_dispo_medico', $selected_id);
	if(is_array($old_data)) {
		$old_data = array_map('makeSafe', $old_data);
		$old_data['selectedID'] = makeSafe($selected_id);
	}

	$data['selectedID'] = makeSafe($selected_id);

	// hook: precio_dispo_medico_before_update
	if(function_exists('precio_dispo_medico_before_update')) {
		$args = ['old_data' => $old_data];
		if(!precio_dispo_medico_before_update($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$set = $data; unset($set['selectedID']);
	foreach ($set as $field => $value) {
		$set[$field] = ($value !== '' && $value !== NULL) ? $value : NULL;
	}

	if(!update(
		'precio_dispo_medico', 
		backtick_keys_once($set), 
		['`id`' => $selected_id], 
		$error_message
	)) {
		echo $error_message;
		echo '<a href="precio_dispo_medico_view.php?SelectedID=' . urlencode($selected_id) . "\">{$Translation['< back']}</a>";
		exit;
	}


	$eo = ['silentErrors' => true];

	update_calc_fields('precio_dispo_medico', $data['selectedID'], calculated_fields()['precio_dispo_medico']);

	// hook: precio_dispo_medico_after_update
	if(function_exists('precio_dispo_medico_after_update')) {
		$res = sql("SELECT * FROM `precio_dispo_medico` WHERE `id`='{$data['selectedID']}' LIMIT 1", $eo);
		if($row = db_fetch_assoc($res)) $data = array_map('makeSafe', $row);

		$data['selectedID'] = $data['id'];
		$args = ['old_data' => $old_data];
		if(!precio_dispo_medico_after_update($data, getMemberInfo(), $args)) return;
	}

	// mm: update ownership data
	sql("UPDATE `membership_userrecords` SET `dateUpdated`='" . time() . "' WHERE `tableName`='precio_dispo_medico' AND `pkValue`='" . makeSafe($selected_id) . "'", $eo);
}

function precio_dispo_medico_form($selected_id = '', $AllowUpdate = 1, $AllowInsert = 1, $AllowDelete = 1, $ShowCancel = 0, $TemplateDV = '', $TemplateDVP = '') {
	// function to return an editable form for a table records
	// and fill it with data of record whose ID is $selected_id. If $selected_id
	// is empty, an empty form is shown, with only an 'Add New'
	// button displayed.

	global $Translation;

	// mm: get table permissions
	$arrPerm = getTablePermissions('precio_dispo_medico');
	if(!$arrPerm['insert'] && $selected_id=='') return $Translation['tableAccessDenied'];
	$AllowInsert = ($arrPerm['insert'] ? true : false);
	// print preview?
	$dvprint = false;
	if($selected_id && $_REQUEST['dvprint_x'] != '') {
		$dvprint = true;
	}

	$filterer_dispo_medico = thisOr($_REQUEST['filterer_dispo_medico'], '');

	// populate filterers, starting from children to grand-parents

	// unique random identifier
	$rnd1 = ($dvprint ? rand(1000000, 9999999) : '');
	// combobox: dispo_medico
	$combo_dispo_medico = new DataCombo;

	if($selected_id) {
		// mm: check member permissions
		if(!$arrPerm['view']) return $Translation['tableAccessDenied'];

		// mm: who is the owner?
		$ownerGroupID = sqlValue("SELECT `groupID` FROM `membership_userrecords` WHERE `tableName`='precio_dispo_medico' AND `pkValue`='" . makeSafe($selected_id) . "'");
		$ownerMemberID = sqlValue("SELECT LCASE(`memberID`) FROM `membership_userrecords` WHERE `tableName`='precio_dispo_medico' AND `pkValue`='" . makeSafe($selected_id) . "'");

		if($arrPerm['view'] == 1 && getLoggedMemberID() != $ownerMemberID) return $Translation['tableAccessDenied'];
		if($arrPerm['view'] == 2 && getLoggedGroupID() != $ownerGroupID) return $Translation['tableAccessDenied'];

		// can edit?
		$AllowUpdate = 0;
		if(($arrPerm['edit'] == 1 && $ownerMemberID == getLoggedMemberID()) || ($arrPerm['edit'] == 2 && $ownerGroupID == getLoggedGroupID()) || $arrPerm['edit'] == 3) {
			$AllowUpdate = 1;
		}

		$res = sql("SELECT * FROM `precio_dispo_medico` WHERE `id`='" . makeSafe($selected_id) . "'", $eo);
		if(!($row = db_fetch_array($res))) {
			return error_message($Translation['No records found'], 'precio_dispo_medico_view.php', false);
		}
		$combo_dispo_medico->SelectedData = $row['dispo_medico'];
		$urow = $row; /* unsanitized data */
		$row = array_map('safe_html', $row);
	} else {
		$combo_dispo_medico->SelectedData = $filterer_dispo_medico;
	}
	$combo_dispo_medico->HTML = '<span id="dispo_medico-container' . $rnd1 . '"></span><input type="hidden" name="dispo_medico" id="dispo_medico' . $rnd1 . '" value="' . html_attr($combo_dispo_medico->SelectedData) . '">';
	$combo_dispo_medico->MatchText = '<span id="dispo_medico-container-readonly' . $rnd1 . '"></span><input type="hidden" name="dispo_medico" id="dispo_medico' . $rnd1 . '" value="' . html_attr($combo_dispo_medico->SelectedData) . '">';

	ob_start();
	?>

	<script>
		// initial lookup values
		AppGini.current_dispo_medico__RAND__ = { text: "", value: "<?php echo addslashes($selected_id ? $urow['dispo_medico'] : htmlspecialchars($filterer_dispo_medico, ENT_QUOTES)); ?>"};

		jQuery(function() {
			setTimeout(function() {
				if(typeof(dispo_medico_reload__RAND__) == 'function') dispo_medico_reload__RAND__();
			}, 50); /* we need to slightly delay client-side execution of the above code to allow AppGini.ajaxCache to work */
		});
		function dispo_medico_reload__RAND__() {
		<?php if(($AllowUpdate || $AllowInsert) && !$dvprint) { ?>

			$j("#dispo_medico-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c) {
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_dispo_medico__RAND__.value, t: 'precio_dispo_medico', f: 'dispo_medico' },
						success: function(resp) {
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="dispo_medico"]').val(resp.results[0].id);
							$j('[id=dispo_medico-container-readonly__RAND__]').html('<span id="dispo_medico-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=compra_dispo_view_parent]').hide(); } else { $j('.btn[id=compra_dispo_view_parent]').show(); }


							if(typeof(dispo_medico_update_autofills__RAND__) == 'function') dispo_medico_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term) { return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page) { return { s: term, p: page, t: 'precio_dispo_medico', f: 'dispo_medico' }; },
					results: function(resp, page) { return resp; }
				},
				escapeMarkup: function(str) { return str; }
			}).on('change', function(e) {
				AppGini.current_dispo_medico__RAND__.value = e.added.id;
				AppGini.current_dispo_medico__RAND__.text = e.added.text;
				$j('[name="dispo_medico"]').val(e.added.id);
				if(e.added.id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=compra_dispo_view_parent]').hide(); } else { $j('.btn[id=compra_dispo_view_parent]').show(); }


				if(typeof(dispo_medico_update_autofills__RAND__) == 'function') dispo_medico_update_autofills__RAND__();
			});

			if(!$j("#dispo_medico-container__RAND__").length) {
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_dispo_medico__RAND__.value, t: 'precio_dispo_medico', f: 'dispo_medico' },
					success: function(resp) {
						$j('[name="dispo_medico"]').val(resp.results[0].id);
						$j('[id=dispo_medico-container-readonly__RAND__]').html('<span id="dispo_medico-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=compra_dispo_view_parent]').hide(); } else { $j('.btn[id=compra_dispo_view_parent]').show(); }

						if(typeof(dispo_medico_update_autofills__RAND__) == 'function') dispo_medico_update_autofills__RAND__();
					}
				});
			}

		<?php } else { ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_dispo_medico__RAND__.value, t: 'precio_dispo_medico', f: 'dispo_medico' },
				success: function(resp) {
					$j('[id=dispo_medico-container__RAND__], [id=dispo_medico-container-readonly__RAND__]').html('<span id="dispo_medico-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=compra_dispo_view_parent]').hide(); } else { $j('.btn[id=compra_dispo_view_parent]').show(); }

					if(typeof(dispo_medico_update_autofills__RAND__) == 'function') dispo_medico_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
	</script>
	<?php

	$lookups = str_replace('__RAND__', $rnd1, ob_get_contents());
	ob_end_clean();


	// code for template based detail view forms

	// open the detail view template
	if($dvprint) {
		$template_file = is_file("./{$TemplateDVP}") ? "./{$TemplateDVP}" : './templates/precio_dispo_medico_templateDVP.html';
		$templateCode = @file_get_contents($template_file);
	} else {
		$template_file = is_file("./{$TemplateDV}") ? "./{$TemplateDV}" : './templates/precio_dispo_medico_templateDV.html';
		$templateCode = @file_get_contents($template_file);
	}

	// process form title
	$templateCode = str_replace('<%%DETAIL_VIEW_TITLE%%>', 'Precio dispo medico details', $templateCode);
	$templateCode = str_replace('<%%RND1%%>', $rnd1, $templateCode);
	$templateCode = str_replace('<%%EMBEDDED%%>', ($_REQUEST['Embedded'] ? 'Embedded=1' : ''), $templateCode);
	// process buttons
	if($AllowInsert) {
		if(!$selected_id) $templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-success" id="insert" name="insert_x" value="1" onclick="return precio_dispo_medico_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save New'] . '</button>', $templateCode);
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="insert" name="insert_x" value="1" onclick="return precio_dispo_medico_validateData();"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save As Copy'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '', $templateCode);
	}

	// 'Back' button action
	if($_REQUEST['Embedded']) {
		$backAction = 'AppGini.closeParentModal(); return false;';
	} else {
		$backAction = '$j(\'form\').eq(0).attr(\'novalidate\', \'novalidate\'); document.myform.reset(); return true;';
	}

	if($selected_id) {
		if(!$_REQUEST['Embedded']) $templateCode = str_replace('<%%DVPRINT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="dvprint" name="dvprint_x" value="1" onclick="$j(\'form\').eq(0).prop(\'novalidate\', true); document.myform.reset(); return true;" title="' . html_attr($Translation['Print Preview']) . '"><i class="glyphicon glyphicon-print"></i> ' . $Translation['Print Preview'] . '</button>', $templateCode);
		if($AllowUpdate) {
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '<button type="submit" class="btn btn-success btn-lg" id="update" name="update_x" value="1" onclick="return precio_dispo_medico_validateData();" title="' . html_attr($Translation['Save Changes']) . '"><i class="glyphicon glyphicon-ok"></i> ' . $Translation['Save Changes'] . '</button>', $templateCode);
		} else {
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		}
		if(($arrPerm[4]==1 && $ownerMemberID==getLoggedMemberID()) || ($arrPerm[4]==2 && $ownerGroupID==getLoggedGroupID()) || $arrPerm[4]==3) { // allow delete?
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '<button type="submit" class="btn btn-danger" id="delete" name="delete_x" value="1" onclick="return confirm(\'' . $Translation['are you sure?'] . '\');" title="' . html_attr($Translation['Delete']) . '"><i class="glyphicon glyphicon-trash"></i> ' . $Translation['Delete'] . '</button>', $templateCode);
		} else {
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);
		}
		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', ($ShowCancel ? '<button type="submit" class="btn btn-default" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>' : ''), $templateCode);
	}

	// set records to read only if user can't insert new records and can't edit current record
	if(($selected_id && !$AllowUpdate && !$AllowInsert) || (!$selected_id && !$AllowInsert)) {
		$jsReadOnly = '';
		$jsReadOnly .= "\tjQuery('#dispo_medico').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\tjQuery('#dispo_medico_caption').prop('disabled', true).css({ color: '#555', backgroundColor: 'white' });\n";
		$jsReadOnly .= "\tjQuery('#precio').replaceWith('<div class=\"form-control-static\" id=\"precio\">' + (jQuery('#precio').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('#proveedor').replaceWith('<div class=\"form-control-static\" id=\"proveedor\">' + (jQuery('#proveedor').val() || '') + '</div>');\n";
		$jsReadOnly .= "\tjQuery('.select2-container').hide();\n";

		$noUploads = true;
	} elseif($AllowInsert) {
		$jsEditable = "\tjQuery('form').eq(0).data('already_changed', true);"; // temporarily disable form change handler
		$jsEditable .= "\tjQuery('form').eq(0).data('already_changed', false);"; // re-enable form change handler
	}

	// process combos
	$templateCode = str_replace('<%%COMBO(dispo_medico)%%>', $combo_dispo_medico->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(dispo_medico)%%>', $combo_dispo_medico->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(dispo_medico)%%>', urlencode($combo_dispo_medico->MatchText), $templateCode);

	/* lookup fields array: 'lookup field name' => array('parent table name', 'lookup field caption') */
	$lookup_fields = array('dispo_medico' => array('compra_dispo', 'Dispo medico'), );
	foreach($lookup_fields as $luf => $ptfc) {
		$pt_perm = getTablePermissions($ptfc[0]);

		// process foreign key links
		if($pt_perm['view'] || $pt_perm['edit']) {
			$templateCode = str_replace("<%%PLINK({$luf})%%>", '<button type="button" class="btn btn-default view_parent hspacer-md" id="' . $ptfc[0] . '_view_parent" title="' . html_attr($Translation['View'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-eye-open"></i></button>', $templateCode);
		}

		// if user has insert permission to parent table of a lookup field, put an add new button
		if($pt_perm['insert'] /* && !$_REQUEST['Embedded']*/) {
			$templateCode = str_replace("<%%ADDNEW({$ptfc[0]})%%>", '<button type="button" class="btn btn-success add_new_parent hspacer-md" id="' . $ptfc[0] . '_add_new" title="' . html_attr($Translation['Add New'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-plus-sign"></i></button>', $templateCode);
		}
	}

	// process images
	$templateCode = str_replace('<%%UPLOADFILE(id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(dispo_medico)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(precio)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(proveedor)%%>', '', $templateCode);

	// process values
	if($selected_id) {
		if( $dvprint) $templateCode = str_replace('<%%VALUE(id)%%>', safe_html($urow['id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(id)%%>', html_attr($row['id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(id)%%>', urlencode($urow['id']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(dispo_medico)%%>', safe_html($urow['dispo_medico']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(dispo_medico)%%>', html_attr($row['dispo_medico']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(dispo_medico)%%>', urlencode($urow['dispo_medico']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(precio)%%>', safe_html($urow['precio']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(precio)%%>', html_attr($row['precio']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(precio)%%>', urlencode($urow['precio']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(proveedor)%%>', safe_html($urow['proveedor']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(proveedor)%%>', html_attr($row['proveedor']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(proveedor)%%>', urlencode($urow['proveedor']), $templateCode);
	} else {
		$templateCode = str_replace('<%%VALUE(id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(dispo_medico)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(dispo_medico)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(precio)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(precio)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(proveedor)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(proveedor)%%>', urlencode(''), $templateCode);
	}

	// process translations
	$templateCode = parseTemplate($templateCode);

	// clear scrap
	$templateCode = str_replace('<%%', '<!-- ', $templateCode);
	$templateCode = str_replace('%%>', ' -->', $templateCode);

	// hide links to inaccessible tables
	if($_REQUEST['dvprint_x'] == '') {
		$templateCode .= "\n\n<script>\$j(function() {\n";
		$arrTables = getTableList();
		foreach($arrTables as $name => $caption) {
			$templateCode .= "\t\$j('#{$name}_link').removeClass('hidden');\n";
			$templateCode .= "\t\$j('#xs_{$name}_link').removeClass('hidden');\n";
		}

		$templateCode .= $jsReadOnly;
		$templateCode .= $jsEditable;

		if(!$selected_id) {
		}

		$templateCode.="\n});</script>\n";
	}

	// ajaxed auto-fill fields
	$templateCode .= '<script>';
	$templateCode .= '$j(function() {';


	$templateCode.="});";
	$templateCode.="</script>";
	$templateCode .= $lookups;

	// handle enforced parent values for read-only lookup fields

	// don't include blank images in lightbox gallery
	$templateCode = preg_replace('/blank.gif" data-lightbox=".*?"/', 'blank.gif"', $templateCode);

	// don't display empty email links
	$templateCode=preg_replace('/<a .*?href="mailto:".*?<\/a>/', '', $templateCode);

	/* default field values */
	$rdata = $jdata = get_defaults('precio_dispo_medico');
	if($selected_id) {
		$jdata = get_joined_record('precio_dispo_medico', $selected_id);
		if($jdata === false) $jdata = get_defaults('precio_dispo_medico');
		$rdata = $row;
	}
	$templateCode .= loadView('precio_dispo_medico-ajax-cache', array('rdata' => $rdata, 'jdata' => $jdata));

	// hook: precio_dispo_medico_dv
	if(function_exists('precio_dispo_medico_dv')) {
		$args=[];
		precio_dispo_medico_dv(($selected_id ? $selected_id : FALSE), getMemberInfo(), $templateCode, $args);
	}

	return $templateCode;
}