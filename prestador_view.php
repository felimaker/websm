<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/prestador.php");
	include_once("{$currDir}/prestador_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('prestador');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'prestador';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`prestador`.`id`" => "id",
		"`prestador`.`nombre`" => "nombre",
		"IF(    CHAR_LENGTH(`tipo_razon_social1`.`razon_social`), CONCAT_WS('',   `tipo_razon_social1`.`razon_social`), '') /* Razon social */" => "razon_social",
		"IF(    CHAR_LENGTH(`tipo_razon_social1`.`nit`), CONCAT_WS('',   `tipo_razon_social1`.`nit`), '') /* Nit */" => "nit",
		"`prestador`.`descripcion`" => "descripcion",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`prestador`.`id`',
		2 => 2,
		3 => '`tipo_razon_social1`.`razon_social`',
		4 => '`tipo_razon_social1`.`nit`',
		5 => 5,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`prestador`.`id`" => "id",
		"`prestador`.`nombre`" => "nombre",
		"IF(    CHAR_LENGTH(`tipo_razon_social1`.`razon_social`), CONCAT_WS('',   `tipo_razon_social1`.`razon_social`), '') /* Razon social */" => "razon_social",
		"IF(    CHAR_LENGTH(`tipo_razon_social1`.`nit`), CONCAT_WS('',   `tipo_razon_social1`.`nit`), '') /* Nit */" => "nit",
		"`prestador`.`descripcion`" => "descripcion",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`prestador`.`id`" => "ID",
		"`prestador`.`nombre`" => "Nombre",
		"IF(    CHAR_LENGTH(`tipo_razon_social1`.`razon_social`), CONCAT_WS('',   `tipo_razon_social1`.`razon_social`), '') /* Razon social */" => "Razon social",
		"IF(    CHAR_LENGTH(`tipo_razon_social1`.`nit`), CONCAT_WS('',   `tipo_razon_social1`.`nit`), '') /* Nit */" => "Nit",
		"`prestador`.`descripcion`" => "Descripcion",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`prestador`.`id`" => "id",
		"`prestador`.`nombre`" => "nombre",
		"IF(    CHAR_LENGTH(`tipo_razon_social1`.`razon_social`), CONCAT_WS('',   `tipo_razon_social1`.`razon_social`), '') /* Razon social */" => "razon_social",
		"IF(    CHAR_LENGTH(`tipo_razon_social1`.`nit`), CONCAT_WS('',   `tipo_razon_social1`.`nit`), '') /* Nit */" => "nit",
		"`prestador`.`descripcion`" => "descripcion",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['razon_social' => 'Razon social', ];

	$x->QueryFrom = "`prestador` LEFT JOIN `tipo_razon_social` as tipo_razon_social1 ON `tipo_razon_social1`.`id`=`prestador`.`razon_social` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'prestador_view.php';
	$x->RedirectAfterInsert = 'prestador_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Prestador';
	$x->TableIcon = 'resources/table_icons/award_star_add.png';
	$x->PrimaryKey = '`prestador`.`id`';

	$x->ColWidth = [150, 150, 150, 150, ];
	$x->ColCaption = ['Nombre', 'Razon social', 'Nit', 'Descripcion', ];
	$x->ColFieldName = ['nombre', 'razon_social', 'nit', 'descripcion', ];
	$x->ColNumber  = [2, 3, 4, 5, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/prestador_templateTV.html';
	$x->SelectedTemplate = 'templates/prestador_templateTVS.html';
	$x->TemplateDV = 'templates/prestador_templateDV.html';
	$x->TemplateDVP = 'templates/prestador_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: prestador_init
	$render = true;
	if(function_exists('prestador_init')) {
		$args = [];
		$render = prestador_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: prestador_header
	$headerCode = '';
	if(function_exists('prestador_header')) {
		$args = [];
		$headerCode = prestador_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: prestador_footer
	$footerCode = '';
	if(function_exists('prestador_footer')) {
		$args = [];
		$footerCode = prestador_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
