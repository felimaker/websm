<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/tipo_parentesco.php");
	include_once("{$currDir}/tipo_parentesco_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('tipo_parentesco');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'tipo_parentesco';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`tipo_parentesco`.`id`" => "id",
		"`tipo_parentesco`.`parentesco`" => "parentesco",
		"`tipo_parentesco`.`descripcion`" => "descripcion",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`tipo_parentesco`.`id`',
		2 => 2,
		3 => 3,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`tipo_parentesco`.`id`" => "id",
		"`tipo_parentesco`.`parentesco`" => "parentesco",
		"`tipo_parentesco`.`descripcion`" => "descripcion",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`tipo_parentesco`.`id`" => "ID",
		"`tipo_parentesco`.`parentesco`" => "Parentesco",
		"`tipo_parentesco`.`descripcion`" => "Descripcion",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`tipo_parentesco`.`id`" => "id",
		"`tipo_parentesco`.`parentesco`" => "parentesco",
		"`tipo_parentesco`.`descripcion`" => "descripcion",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`tipo_parentesco` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'tipo_parentesco_view.php';
	$x->RedirectAfterInsert = 'tipo_parentesco_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Tipo de parentesco';
	$x->TableIcon = 'resources/table_icons/cog_add.png';
	$x->PrimaryKey = '`tipo_parentesco`.`id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Parentesco', 'Descripcion', ];
	$x->ColFieldName = ['parentesco', 'descripcion', ];
	$x->ColNumber  = [2, 3, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/tipo_parentesco_templateTV.html';
	$x->SelectedTemplate = 'templates/tipo_parentesco_templateTVS.html';
	$x->TemplateDV = 'templates/tipo_parentesco_templateDV.html';
	$x->TemplateDVP = 'templates/tipo_parentesco_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: tipo_parentesco_init
	$render = true;
	if(function_exists('tipo_parentesco_init')) {
		$args = [];
		$render = tipo_parentesco_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: tipo_parentesco_header
	$headerCode = '';
	if(function_exists('tipo_parentesco_header')) {
		$args = [];
		$headerCode = tipo_parentesco_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: tipo_parentesco_footer
	$footerCode = '';
	if(function_exists('tipo_parentesco_footer')) {
		$args = [];
		$footerCode = tipo_parentesco_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
