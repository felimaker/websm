<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/cantidad_dispomedico.php");
	include_once("{$currDir}/cantidad_dispomedico_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('cantidad_dispomedico');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'cantidad_dispomedico';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`cantidad_dispomedico`.`id`" => "id",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`nombre`), CONCAT_WS('',   `compra_dispo1`.`nombre`), '') /* Nombre del dispositivo */" => "nombre_dispo",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`serial`), CONCAT_WS('',   `compra_dispo1`.`serial`), '') /* Serial */" => "serial",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`) || CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marcas1`.`marca`, ' ', `marca_modelo1`.`modelo`), '') /* Marca y Modelo */" => "marca_modelo",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`caracteristicas`) || CHAR_LENGTH(`marca_modelo2`.`modelo`), CONCAT_WS('',   `compra_dispo1`.`caracteristicas`, ' ', `marca_modelo2`.`modelo`), '') /* Descripcion del dispositivo */" => "descripcion",
		"`cantidad_dispomedico`.`uso`" => "uso",
		"IF(    CHAR_LENGTH(`tipo_estado_compra1`.`tipo_estado_compra`), CONCAT_WS('',   `tipo_estado_compra1`.`tipo_estado_compra`), '') /* Estado */" => "estado",
		"IF(    CHAR_LENGTH(`compras1`.`id`), CONCAT_WS('',   `compras1`.`id`), '') /* Id compra */" => "id_compra",
		"if(`cantidad_dispomedico`.`creado`,date_format(`cantidad_dispomedico`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`cantidad_dispomedico`.`creado_por`" => "creado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`cantidad_dispomedico`.`id`',
		2 => '`compra_dispo1`.`nombre`',
		3 => '`compra_dispo1`.`serial`',
		4 => 4,
		5 => 5,
		6 => 6,
		7 => '`tipo_estado_compra1`.`tipo_estado_compra`',
		8 => '`compras1`.`id`',
		9 => '`cantidad_dispomedico`.`creado`',
		10 => 10,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`cantidad_dispomedico`.`id`" => "id",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`nombre`), CONCAT_WS('',   `compra_dispo1`.`nombre`), '') /* Nombre del dispositivo */" => "nombre_dispo",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`serial`), CONCAT_WS('',   `compra_dispo1`.`serial`), '') /* Serial */" => "serial",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`) || CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marcas1`.`marca`, ' ', `marca_modelo1`.`modelo`), '') /* Marca y Modelo */" => "marca_modelo",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`caracteristicas`) || CHAR_LENGTH(`marca_modelo2`.`modelo`), CONCAT_WS('',   `compra_dispo1`.`caracteristicas`, ' ', `marca_modelo2`.`modelo`), '') /* Descripcion del dispositivo */" => "descripcion",
		"`cantidad_dispomedico`.`uso`" => "uso",
		"IF(    CHAR_LENGTH(`tipo_estado_compra1`.`tipo_estado_compra`), CONCAT_WS('',   `tipo_estado_compra1`.`tipo_estado_compra`), '') /* Estado */" => "estado",
		"IF(    CHAR_LENGTH(`compras1`.`id`), CONCAT_WS('',   `compras1`.`id`), '') /* Id compra */" => "id_compra",
		"if(`cantidad_dispomedico`.`creado`,date_format(`cantidad_dispomedico`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`cantidad_dispomedico`.`creado_por`" => "creado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`cantidad_dispomedico`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`nombre`), CONCAT_WS('',   `compra_dispo1`.`nombre`), '') /* Nombre del dispositivo */" => "Nombre del dispositivo",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`serial`), CONCAT_WS('',   `compra_dispo1`.`serial`), '') /* Serial */" => "Serial",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`) || CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marcas1`.`marca`, ' ', `marca_modelo1`.`modelo`), '') /* Marca y Modelo */" => "Marca y Modelo",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`caracteristicas`) || CHAR_LENGTH(`marca_modelo2`.`modelo`), CONCAT_WS('',   `compra_dispo1`.`caracteristicas`, ' ', `marca_modelo2`.`modelo`), '') /* Descripcion del dispositivo */" => "Descripcion del dispositivo",
		"`cantidad_dispomedico`.`uso`" => "Uso",
		"IF(    CHAR_LENGTH(`tipo_estado_compra1`.`tipo_estado_compra`), CONCAT_WS('',   `tipo_estado_compra1`.`tipo_estado_compra`), '') /* Estado */" => "Estado",
		"IF(    CHAR_LENGTH(`compras1`.`id`), CONCAT_WS('',   `compras1`.`id`), '') /* Id compra */" => "Id compra",
		"`cantidad_dispomedico`.`creado`" => "Creado",
		"`cantidad_dispomedico`.`creado_por`" => "Creado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`cantidad_dispomedico`.`id`" => "id",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`nombre`), CONCAT_WS('',   `compra_dispo1`.`nombre`), '') /* Nombre del dispositivo */" => "nombre_dispo",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`serial`), CONCAT_WS('',   `compra_dispo1`.`serial`), '') /* Serial */" => "serial",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`) || CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marcas1`.`marca`, ' ', `marca_modelo1`.`modelo`), '') /* Marca y Modelo */" => "marca_modelo",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`caracteristicas`) || CHAR_LENGTH(`marca_modelo2`.`modelo`), CONCAT_WS('',   `compra_dispo1`.`caracteristicas`, ' ', `marca_modelo2`.`modelo`), '') /* Descripcion del dispositivo */" => "descripcion",
		"`cantidad_dispomedico`.`uso`" => "uso",
		"IF(    CHAR_LENGTH(`tipo_estado_compra1`.`tipo_estado_compra`), CONCAT_WS('',   `tipo_estado_compra1`.`tipo_estado_compra`), '') /* Estado */" => "estado",
		"IF(    CHAR_LENGTH(`compras1`.`id`), CONCAT_WS('',   `compras1`.`id`), '') /* Id compra */" => "id_compra",
		"if(`cantidad_dispomedico`.`creado`,date_format(`cantidad_dispomedico`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`cantidad_dispomedico`.`creado_por`" => "creado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['nombre_dispo' => 'Nombre del dispositivo', 'estado' => 'Estado', 'id_compra' => 'Id compra', ];

	$x->QueryFrom = "`cantidad_dispomedico` LEFT JOIN `compra_dispo` as compra_dispo1 ON `compra_dispo1`.`id`=`cantidad_dispomedico`.`nombre_dispo` LEFT JOIN `tipo_estado_compra` as tipo_estado_compra1 ON `tipo_estado_compra1`.`id`=`cantidad_dispomedico`.`estado` LEFT JOIN `compras` as compras1 ON `compras1`.`id`=`cantidad_dispomedico`.`id_compra` LEFT JOIN `marcas` as marcas1 ON `marcas1`.`id_marca`=`compra_dispo1`.`marca` LEFT JOIN `marca_modelo` as marca_modelo1 ON `marca_modelo1`.`id_mmodelo`=`compra_dispo1`.`modelo` LEFT JOIN `marca_modelo` as marca_modelo2 ON `marca_modelo2`.`id_mmodelo`=`compra_dispo1`.`modelo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'cantidad_dispomedico_view.php';
	$x->RedirectAfterInsert = 'cantidad_dispomedico_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Dispositivos Biomedicos Nuevos';
	$x->TableIcon = 'resources/table_icons/basket_add.png';
	$x->PrimaryKey = '`cantidad_dispomedico`.`id`';
	$x->DefaultSortField = '`cantidad_dispomedico`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Nombre del dispositivo', 'Serial', 'Marca y Modelo', 'Descripcion del dispositivo', 'Uso', 'Estado', ];
	$x->ColFieldName = ['nombre_dispo', 'serial', 'marca_modelo', 'descripcion', 'uso', 'estado', ];
	$x->ColNumber  = [2, 3, 4, 5, 6, 7, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/cantidad_dispomedico_templateTV.html';
	$x->SelectedTemplate = 'templates/cantidad_dispomedico_templateTVS.html';
	$x->TemplateDV = 'templates/cantidad_dispomedico_templateDV.html';
	$x->TemplateDVP = 'templates/cantidad_dispomedico_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: cantidad_dispomedico_init
	$render = true;
	if(function_exists('cantidad_dispomedico_init')) {
		$args = [];
		$render = cantidad_dispomedico_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: cantidad_dispomedico_header
	$headerCode = '';
	if(function_exists('cantidad_dispomedico_header')) {
		$args = [];
		$headerCode = cantidad_dispomedico_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: cantidad_dispomedico_footer
	$footerCode = '';
	if(function_exists('cantidad_dispomedico_footer')) {
		$args = [];
		$footerCode = cantidad_dispomedico_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
