<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/movimientos.php");
	include_once("{$currDir}/movimientos_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('movimientos');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'movimientos';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`movimientos`.`id_movi`" => "id_movi",
		"`movimientos`.`img`" => "img",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo */" => "tipo_dispo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "codigo_dispo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "serial_dispo",
		"IF(    CHAR_LENGTH(`tipo_estado_movi1`.`estado_movi`), CONCAT_WS('',   `tipo_estado_movi1`.`estado_movi`), '') /* Estado */" => "estado_movi",
		"`movimientos`.`origen`" => "origen",
		"`movimientos`.`destino`" => "destino",
		"if(`movimientos`.`creado`,date_format(`movimientos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`movimientos`.`creado_por`" => "creado_por",
		"if(`movimientos`.`editado`,date_format(`movimientos`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`movimientos`.`editado_por`" => "editado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`movimientos`.`id_movi`',
		2 => '`movimientos`.`img`',
		3 => 3,
		4 => '`dispositivos1`.`code`',
		5 => '`dispositivos1`.`serial`',
		6 => '`tipo_estado_movi1`.`estado_movi`',
		7 => 7,
		8 => 8,
		9 => '`movimientos`.`creado`',
		10 => 10,
		11 => '`movimientos`.`editado`',
		12 => 12,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`movimientos`.`id_movi`" => "id_movi",
		"`movimientos`.`img`" => "img",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo */" => "tipo_dispo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "codigo_dispo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "serial_dispo",
		"IF(    CHAR_LENGTH(`tipo_estado_movi1`.`estado_movi`), CONCAT_WS('',   `tipo_estado_movi1`.`estado_movi`), '') /* Estado */" => "estado_movi",
		"`movimientos`.`origen`" => "origen",
		"`movimientos`.`destino`" => "destino",
		"if(`movimientos`.`creado`,date_format(`movimientos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`movimientos`.`creado_por`" => "creado_por",
		"if(`movimientos`.`editado`,date_format(`movimientos`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`movimientos`.`editado_por`" => "editado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`movimientos`.`id_movi`" => "ID",
		"`movimientos`.`img`" => "Img",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo */" => "Tipo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "Codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "Serial",
		"IF(    CHAR_LENGTH(`tipo_estado_movi1`.`estado_movi`), CONCAT_WS('',   `tipo_estado_movi1`.`estado_movi`), '') /* Estado */" => "Estado",
		"`movimientos`.`origen`" => "Origen",
		"`movimientos`.`destino`" => "Destino",
		"`movimientos`.`creado`" => "Fecha de verificaci&#243;n",
		"`movimientos`.`creado_por`" => "Creado por",
		"`movimientos`.`editado`" => "Editado",
		"`movimientos`.`editado_por`" => "Editado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`movimientos`.`id_movi`" => "id_movi",
		"`movimientos`.`img`" => "img",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo */" => "tipo_dispo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "codigo_dispo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "serial_dispo",
		"IF(    CHAR_LENGTH(`tipo_estado_movi1`.`estado_movi`), CONCAT_WS('',   `tipo_estado_movi1`.`estado_movi`), '') /* Estado */" => "estado_movi",
		"`movimientos`.`origen`" => "origen",
		"`movimientos`.`destino`" => "destino",
		"if(`movimientos`.`creado`,date_format(`movimientos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`movimientos`.`creado_por`" => "creado_por",
		"if(`movimientos`.`editado`,date_format(`movimientos`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`movimientos`.`editado_por`" => "editado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['codigo_dispo' => 'Codigo', 'estado_movi' => 'Estado', ];

	$x->QueryFrom = "`movimientos` LEFT JOIN `dispositivos` as dispositivos1 ON `dispositivos1`.`id_dispo`=`movimientos`.`codigo_dispo` LEFT JOIN `tipo_estado_movi` as tipo_estado_movi1 ON `tipo_estado_movi1`.`id`=`movimientos`.`estado_movi` LEFT JOIN `tipo_dispositivo` as tipo_dispositivo1 ON `tipo_dispositivo1`.`id_tipodispo`=`dispositivos1`.`tipo_dispositivo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = (getLoggedAdmin() !== false);
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'movimientos_view.php';
	$x->RedirectAfterInsert = 'movimientos_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Movimientos';
	$x->TableIcon = 'resources/table_icons/arrow_branch.png';
	$x->PrimaryKey = '`movimientos`.`id_movi`';
	$x->DefaultSortField = '`movimientos`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Tipo', 'Codigo', 'Estado', 'Origen', 'Destino', 'Fecha de verificaci&#243;n', 'Creado por', ];
	$x->ColFieldName = ['tipo_dispo', 'codigo_dispo', 'estado_movi', 'origen', 'destino', 'creado', 'creado_por', ];
	$x->ColNumber  = [3, 4, 6, 7, 8, 9, 10, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/movimientos_templateTV.html';
	$x->SelectedTemplate = 'templates/movimientos_templateTVS.html';
	$x->TemplateDV = 'templates/movimientos_templateDV.html';
	$x->TemplateDVP = 'templates/movimientos_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = true;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: movimientos_init
	$render = true;
	if(function_exists('movimientos_init')) {
		$args = [];
		$render = movimientos_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: movimientos_header
	$headerCode = '';
	if(function_exists('movimientos_header')) {
		$args = [];
		$headerCode = movimientos_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: movimientos_footer
	$footerCode = '';
	if(function_exists('movimientos_footer')) {
		$args = [];
		$footerCode = movimientos_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
