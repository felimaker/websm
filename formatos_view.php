<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/formatos.php");
	include_once("{$currDir}/formatos_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('formatos');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'formatos';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`formatos`.`id`" => "id",
		"IF(    CHAR_LENGTH(`tipo_formatos1`.`tipo_formato`), CONCAT_WS('',   `tipo_formatos1`.`tipo_formato`), '') /* Tipo formato */" => "tipo_formato",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo de dispositivo */" => "tipo_dispositivo",
		"`formatos`.`nombre_formato`" => "nombre_formato",
		"`formatos`.`formato`" => "formato",
		"`formatos`.`descripcion`" => "descripcion",
		"if(`formatos`.`creado`,date_format(`formatos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`formatos`.`id`',
		2 => '`tipo_formatos1`.`tipo_formato`',
		3 => '`tipo_dispositivo1`.`tipo_dispositivo`',
		4 => 4,
		5 => 5,
		6 => 6,
		7 => '`formatos`.`creado`',
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`formatos`.`id`" => "id",
		"IF(    CHAR_LENGTH(`tipo_formatos1`.`tipo_formato`), CONCAT_WS('',   `tipo_formatos1`.`tipo_formato`), '') /* Tipo formato */" => "tipo_formato",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo de dispositivo */" => "tipo_dispositivo",
		"`formatos`.`nombre_formato`" => "nombre_formato",
		"`formatos`.`formato`" => "formato",
		"`formatos`.`descripcion`" => "descripcion",
		"if(`formatos`.`creado`,date_format(`formatos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`formatos`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`tipo_formatos1`.`tipo_formato`), CONCAT_WS('',   `tipo_formatos1`.`tipo_formato`), '') /* Tipo formato */" => "Tipo formato",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo de dispositivo */" => "Tipo de dispositivo",
		"`formatos`.`nombre_formato`" => "Nombre formato",
		"`formatos`.`formato`" => "Formato",
		"`formatos`.`descripcion`" => "Uso",
		"`formatos`.`creado`" => "Creado",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`formatos`.`id`" => "id",
		"IF(    CHAR_LENGTH(`tipo_formatos1`.`tipo_formato`), CONCAT_WS('',   `tipo_formatos1`.`tipo_formato`), '') /* Tipo formato */" => "tipo_formato",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo de dispositivo */" => "tipo_dispositivo",
		"`formatos`.`nombre_formato`" => "nombre_formato",
		"`formatos`.`formato`" => "formato",
		"`formatos`.`descripcion`" => "descripcion",
		"if(`formatos`.`creado`,date_format(`formatos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['tipo_formato' => 'Tipo formato', 'tipo_dispositivo' => 'Tipo de dispositivo', ];

	$x->QueryFrom = "`formatos` LEFT JOIN `tipo_formatos` as tipo_formatos1 ON `tipo_formatos1`.`id`=`formatos`.`tipo_formato` LEFT JOIN `tipo_dispositivo` as tipo_dispositivo1 ON `tipo_dispositivo1`.`id_tipodispo`=`formatos`.`tipo_dispositivo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'formatos_view.php';
	$x->RedirectAfterInsert = 'formatos_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Formatos';
	$x->TableIcon = 'resources/table_icons/align_right.png';
	$x->PrimaryKey = '`formatos`.`id`';
	$x->DefaultSortField = '`formatos`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Tipo formato', 'Tipo de dispositivo', 'Nombre formato', 'Formato', 'Uso', ];
	$x->ColFieldName = ['tipo_formato', 'tipo_dispositivo', 'nombre_formato', 'formato', 'descripcion', ];
	$x->ColNumber  = [2, 3, 4, 5, 6, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/formatos_templateTV.html';
	$x->SelectedTemplate = 'templates/formatos_templateTVS.html';
	$x->TemplateDV = 'templates/formatos_templateDV.html';
	$x->TemplateDVP = 'templates/formatos_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: formatos_init
	$render = true;
	if(function_exists('formatos_init')) {
		$args = [];
		$render = formatos_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: formatos_header
	$headerCode = '';
	if(function_exists('formatos_header')) {
		$args = [];
		$headerCode = formatos_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: formatos_footer
	$footerCode = '';
	if(function_exists('formatos_footer')) {
		$args = [];
		$footerCode = formatos_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
