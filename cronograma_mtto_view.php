<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/cronograma_mtto.php");
	include_once("{$currDir}/cronograma_mtto_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('cronograma_mtto');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'cronograma_mtto';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`cronograma_mtto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Tipo dispo */" => "id_programa",
		"IF(    CHAR_LENGTH(`programa_mtto1`.`ano`), CONCAT_WS('',   `programa_mtto1`.`ano`), '') /* Ano */" => "ano",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* codigo del dispositivo */" => "codigo",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo de dispositivo */" => "tipo_dispo",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`), CONCAT_WS('',   `marcas1`.`marca`), '') /* Marca */" => "marca",
		"IF(    CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marca_modelo1`.`modelo`), '') /* Modelo */" => "modelo",
		"IF(    CHAR_LENGTH(`tipo_periodicidad1`.`periodicidad`), CONCAT_WS('',   `tipo_periodicidad1`.`periodicidad`), '') /* Periodicidad */" => "periodicidad",
		"`cronograma_mtto`.`meses`" => "meses",
		"`cronograma_mtto`.`biomedico`" => "biomedico",
		"`cronograma_mtto`.`actividades`" => "actividades",
		"if(`cronograma_mtto`.`creado`,date_format(`cronograma_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`cronograma_mtto`.`creado_por`" => "creado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`cronograma_mtto`.`id`',
		2 => 2,
		3 => '`programa_mtto1`.`ano`',
		4 => '`dispositivos1`.`code`',
		5 => 5,
		6 => '`marcas1`.`marca`',
		7 => 7,
		8 => '`tipo_periodicidad1`.`periodicidad`',
		9 => 9,
		10 => 10,
		11 => 11,
		12 => '`cronograma_mtto`.`creado`',
		13 => 13,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`cronograma_mtto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Tipo dispo */" => "id_programa",
		"IF(    CHAR_LENGTH(`programa_mtto1`.`ano`), CONCAT_WS('',   `programa_mtto1`.`ano`), '') /* Ano */" => "ano",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* codigo del dispositivo */" => "codigo",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo de dispositivo */" => "tipo_dispo",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`), CONCAT_WS('',   `marcas1`.`marca`), '') /* Marca */" => "marca",
		"IF(    CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marca_modelo1`.`modelo`), '') /* Modelo */" => "modelo",
		"IF(    CHAR_LENGTH(`tipo_periodicidad1`.`periodicidad`), CONCAT_WS('',   `tipo_periodicidad1`.`periodicidad`), '') /* Periodicidad */" => "periodicidad",
		"`cronograma_mtto`.`meses`" => "meses",
		"`cronograma_mtto`.`biomedico`" => "biomedico",
		"`cronograma_mtto`.`actividades`" => "actividades",
		"if(`cronograma_mtto`.`creado`,date_format(`cronograma_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`cronograma_mtto`.`creado_por`" => "creado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`cronograma_mtto`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Tipo dispo */" => "Tipo dispo",
		"IF(    CHAR_LENGTH(`programa_mtto1`.`ano`), CONCAT_WS('',   `programa_mtto1`.`ano`), '') /* Ano */" => "Ano",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* codigo del dispositivo */" => "codigo del dispositivo",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo de dispositivo */" => "Tipo de dispositivo",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`), CONCAT_WS('',   `marcas1`.`marca`), '') /* Marca */" => "Marca",
		"IF(    CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marca_modelo1`.`modelo`), '') /* Modelo */" => "Modelo",
		"IF(    CHAR_LENGTH(`tipo_periodicidad1`.`periodicidad`), CONCAT_WS('',   `tipo_periodicidad1`.`periodicidad`), '') /* Periodicidad */" => "Periodicidad",
		"`cronograma_mtto`.`meses`" => "Meses",
		"`cronograma_mtto`.`biomedico`" => "Biomedico",
		"`cronograma_mtto`.`actividades`" => "Actividades",
		"`cronograma_mtto`.`creado`" => "Creado",
		"`cronograma_mtto`.`creado_por`" => "Creado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`cronograma_mtto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Tipo dispo */" => "id_programa",
		"IF(    CHAR_LENGTH(`programa_mtto1`.`ano`), CONCAT_WS('',   `programa_mtto1`.`ano`), '') /* Ano */" => "ano",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* codigo del dispositivo */" => "codigo",
		"IF(    CHAR_LENGTH(`tipo_dispositivo1`.`tipo_dispositivo`), CONCAT_WS('',   `tipo_dispositivo1`.`tipo_dispositivo`), '') /* Tipo de dispositivo */" => "tipo_dispo",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`), CONCAT_WS('',   `marcas1`.`marca`), '') /* Marca */" => "marca",
		"IF(    CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marca_modelo1`.`modelo`), '') /* Modelo */" => "modelo",
		"IF(    CHAR_LENGTH(`tipo_periodicidad1`.`periodicidad`), CONCAT_WS('',   `tipo_periodicidad1`.`periodicidad`), '') /* Periodicidad */" => "periodicidad",
		"`cronograma_mtto`.`meses`" => "meses",
		"`cronograma_mtto`.`biomedico`" => "biomedico",
		"`cronograma_mtto`.`actividades`" => "actividades",
		"if(`cronograma_mtto`.`creado`,date_format(`cronograma_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`cronograma_mtto`.`creado_por`" => "creado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['id_programa' => 'Tipo dispo', 'codigo' => 'codigo del dispositivo', 'marca' => 'Marca', 'periodicidad' => 'Periodicidad', ];

	$x->QueryFrom = "`cronograma_mtto` LEFT JOIN `programa_mtto` as programa_mtto1 ON `programa_mtto1`.`id`=`cronograma_mtto`.`id_programa` LEFT JOIN `unidades` as unidades1 ON `unidades1`.`id_unidades`=`programa_mtto1`.`unidad` LEFT JOIN `dispositivos` as dispositivos1 ON `dispositivos1`.`id_dispo`=`cronograma_mtto`.`codigo` LEFT JOIN `marcas` as marcas1 ON `marcas1`.`id_marca`=`cronograma_mtto`.`marca` LEFT JOIN `tipo_periodicidad` as tipo_periodicidad1 ON `tipo_periodicidad1`.`id`=`cronograma_mtto`.`periodicidad` LEFT JOIN `tipo_dispositivo` as tipo_dispositivo1 ON `tipo_dispositivo1`.`id_tipodispo`=`dispositivos1`.`tipo_dispositivo` LEFT JOIN `marca_modelo` as marca_modelo1 ON `marca_modelo1`.`id_mmodelo`=`dispositivos1`.`modelo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'cronograma_mtto_view.php';
	$x->RedirectAfterInsert = 'cronograma_mtto_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Cronograma mtto';
	$x->TableIcon = 'resources/table_icons/application_form_add.png';
	$x->PrimaryKey = '`cronograma_mtto`.`id`';
	$x->DefaultSortField = '`cronograma_mtto`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Ano', 'codigo del dispositivo', 'Tipo de dispositivo', 'Marca', 'Modelo', 'Periodicidad', 'Meses', 'Biomedico', 'Actividades', ];
	$x->ColFieldName = ['ano', 'codigo', 'tipo_dispo', 'marca', 'modelo', 'periodicidad', 'meses', 'biomedico', 'actividades', ];
	$x->ColNumber  = [3, 4, 5, 6, 7, 8, 9, 10, 11, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/cronograma_mtto_templateTV.html';
	$x->SelectedTemplate = 'templates/cronograma_mtto_templateTVS.html';
	$x->TemplateDV = 'templates/cronograma_mtto_templateDV.html';
	$x->TemplateDVP = 'templates/cronograma_mtto_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: cronograma_mtto_init
	$render = true;
	if(function_exists('cronograma_mtto_init')) {
		$args = [];
		$render = cronograma_mtto_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: cronograma_mtto_header
	$headerCode = '';
	if(function_exists('cronograma_mtto_header')) {
		$args = [];
		$headerCode = cronograma_mtto_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: cronograma_mtto_footer
	$footerCode = '';
	if(function_exists('cronograma_mtto_footer')) {
		$args = [];
		$footerCode = cronograma_mtto_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
