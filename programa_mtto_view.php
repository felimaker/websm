<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/programa_mtto.php");
	include_once("{$currDir}/programa_mtto_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('programa_mtto');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'programa_mtto';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`programa_mtto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Unidad */" => "unidad",
		"`programa_mtto`.`ano`" => "ano",
		"`programa_mtto`.`descripcion`" => "descripcion",
		"`programa_mtto`.`enlace`" => "enlace",
		"`programa_mtto`.`archivo`" => "archivo",
		"if(`programa_mtto`.`creado`,date_format(`programa_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`programa_mtto`.`creado_por`" => "creado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`programa_mtto`.`id`',
		2 => '`unidades1`.`nombre`',
		3 => '`programa_mtto`.`ano`',
		4 => 4,
		5 => 5,
		6 => 6,
		7 => '`programa_mtto`.`creado`',
		8 => 8,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`programa_mtto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Unidad */" => "unidad",
		"`programa_mtto`.`ano`" => "ano",
		"`programa_mtto`.`descripcion`" => "descripcion",
		"`programa_mtto`.`enlace`" => "enlace",
		"`programa_mtto`.`archivo`" => "archivo",
		"if(`programa_mtto`.`creado`,date_format(`programa_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`programa_mtto`.`creado_por`" => "creado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`programa_mtto`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Unidad */" => "Unidad",
		"`programa_mtto`.`ano`" => "A&#241;o ",
		"`programa_mtto`.`descripcion`" => "Dscripcion",
		"`programa_mtto`.`enlace`" => "Enlace",
		"`programa_mtto`.`archivo`" => "Archivo",
		"`programa_mtto`.`creado`" => "Creado",
		"`programa_mtto`.`creado_por`" => "Creado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`programa_mtto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Unidad */" => "unidad",
		"`programa_mtto`.`ano`" => "ano",
		"`programa_mtto`.`descripcion`" => "descripcion",
		"`programa_mtto`.`enlace`" => "enlace",
		"`programa_mtto`.`archivo`" => "archivo",
		"if(`programa_mtto`.`creado`,date_format(`programa_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`programa_mtto`.`creado_por`" => "creado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['unidad' => 'Unidad', ];

	$x->QueryFrom = "`programa_mtto` LEFT JOIN `unidades` as unidades1 ON `unidades1`.`id_unidades`=`programa_mtto`.`unidad` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'programa_mtto_view.php';
	$x->RedirectAfterInsert = 'programa_mtto_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Programa mtto';
	$x->TableIcon = 'resources/table_icons/book_add.png';
	$x->PrimaryKey = '`programa_mtto`.`id`';
	$x->DefaultSortField = '`programa_mtto`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Unidad', 'A&#241;o ', 'Dscripcion', 'Enlace', 'Archivo', ];
	$x->ColFieldName = ['unidad', 'ano', 'descripcion', 'enlace', 'archivo', ];
	$x->ColNumber  = [2, 3, 4, 5, 6, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/programa_mtto_templateTV.html';
	$x->SelectedTemplate = 'templates/programa_mtto_templateTVS.html';
	$x->TemplateDV = 'templates/programa_mtto_templateDV.html';
	$x->TemplateDVP = 'templates/programa_mtto_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: programa_mtto_init
	$render = true;
	if(function_exists('programa_mtto_init')) {
		$args = [];
		$render = programa_mtto_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: programa_mtto_header
	$headerCode = '';
	if(function_exists('programa_mtto_header')) {
		$args = [];
		$headerCode = programa_mtto_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: programa_mtto_footer
	$footerCode = '';
	if(function_exists('programa_mtto_footer')) {
		$args = [];
		$footerCode = programa_mtto_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
