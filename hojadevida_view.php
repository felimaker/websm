<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/hojadevida.php");
	include_once("{$currDir}/hojadevida_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('hojadevida');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'hojadevida';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`hojadevida`.`id`" => "id",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "dispo_codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "dispo_serial",
		"`hojadevida`.`hardware`" => "hardware",
		"`hojadevida`.`software`" => "software",
		"`hojadevida`.`carga`" => "carga",
		"if(`hojadevida`.`creado`,date_format(`hojadevida`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`hojadevida`.`creado_por`" => "creado_por",
		"if(`hojadevida`.`editado`,date_format(`hojadevida`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`hojadevida`.`editado_por`" => "editado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`hojadevida`.`id`',
		2 => '`dispositivos1`.`code`',
		3 => '`dispositivos1`.`serial`',
		4 => '`hojadevida`.`hardware`',
		5 => 5,
		6 => 6,
		7 => '`hojadevida`.`creado`',
		8 => 8,
		9 => '`hojadevida`.`editado`',
		10 => 10,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`hojadevida`.`id`" => "id",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "dispo_codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "dispo_serial",
		"`hojadevida`.`hardware`" => "hardware",
		"`hojadevida`.`software`" => "software",
		"`hojadevida`.`carga`" => "carga",
		"if(`hojadevida`.`creado`,date_format(`hojadevida`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`hojadevida`.`creado_por`" => "creado_por",
		"if(`hojadevida`.`editado`,date_format(`hojadevida`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`hojadevida`.`editado_por`" => "editado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`hojadevida`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "Codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "Serial",
		"`hojadevida`.`hardware`" => "Hardware",
		"`hojadevida`.`software`" => "Software",
		"`hojadevida`.`carga`" => "Documento",
		"`hojadevida`.`creado`" => "Creado",
		"`hojadevida`.`creado_por`" => "Creado por",
		"`hojadevida`.`editado`" => "Editado",
		"`hojadevida`.`editado_por`" => "Editado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`hojadevida`.`id`" => "id",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "dispo_codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "dispo_serial",
		"`hojadevida`.`hardware`" => "hardware",
		"`hojadevida`.`software`" => "software",
		"`hojadevida`.`carga`" => "carga",
		"if(`hojadevida`.`creado`,date_format(`hojadevida`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`hojadevida`.`creado_por`" => "creado_por",
		"if(`hojadevida`.`editado`,date_format(`hojadevida`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`hojadevida`.`editado_por`" => "editado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['dispo_codigo' => 'Codigo', ];

	$x->QueryFrom = "`hojadevida` LEFT JOIN `dispositivos` as dispositivos1 ON `dispositivos1`.`id_dispo`=`hojadevida`.`dispo_codigo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = (getLoggedAdmin() !== false);
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'hojadevida_view.php';
	$x->RedirectAfterInsert = 'hojadevida_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Hoja de vida';
	$x->TableIcon = 'resources/table_icons/application_view_list.png';
	$x->PrimaryKey = '`hojadevida`.`id`';
	$x->DefaultSortField = '`hojadevida`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Codigo', 'Serial', 'Hardware', 'Software', 'Documento', 'Creado', 'Editado', ];
	$x->ColFieldName = ['dispo_codigo', 'dispo_serial', 'hardware', 'software', 'carga', 'creado', 'editado', ];
	$x->ColNumber  = [2, 3, 4, 5, 6, 7, 9, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/hojadevida_templateTV.html';
	$x->SelectedTemplate = 'templates/hojadevida_templateTVS.html';
	$x->TemplateDV = 'templates/hojadevida_templateDV.html';
	$x->TemplateDVP = 'templates/hojadevida_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: hojadevida_init
	$render = true;
	if(function_exists('hojadevida_init')) {
		$args = [];
		$render = hojadevida_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: hojadevida_header
	$headerCode = '';
	if(function_exists('hojadevida_header')) {
		$args = [];
		$headerCode = hojadevida_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: hojadevida_footer
	$footerCode = '';
	if(function_exists('hojadevida_footer')) {
		$args = [];
		$footerCode = hojadevida_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
