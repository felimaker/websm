<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/doc_mtto.php");
	include_once("{$currDir}/doc_mtto_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('doc_mtto');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'doc_mtto';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`doc_mtto`.`id_doc`" => "id_doc",
		"`doc_mtto`.`doc`" => "doc",
		"IF(    CHAR_LENGTH(`mantenimientos1`.`id_mtto`), CONCAT_WS('',   `mantenimientos1`.`id_mtto`), '') /* Id mtto */" => "id_mtto",
		"IF(    CHAR_LENGTH(`tipo_mtto1`.`tipo_mtto`), CONCAT_WS('',   `tipo_mtto1`.`tipo_mtto`), '') /* Tipo mtto */" => "tipo_mtto",
		"IF(    CHAR_LENGTH(`mantenimientos1`.`descripcion`), CONCAT_WS('',   `mantenimientos1`.`descripcion`), '') /* Mtto */" => "mtto",
		"`doc_mtto`.`observaciones`" => "observaciones",
		"IF(    CHAR_LENGTH(`biomedicos1`.`nombre`), CONCAT_WS('',   `biomedicos1`.`nombre`), '') /* Nombre profesional */" => "nombre_biomedico",
		"IF(    CHAR_LENGTH(`biomedicos1`.`registro_invima`), CONCAT_WS('',   `biomedicos1`.`registro_invima`), '') /* Invima */" => "registro_invima",
		"IF(    CHAR_LENGTH(`biomedicos1`.`firma`), CONCAT_WS('',   `biomedicos1`.`firma`), '') /* Firma profesional */" => "firma_biomedico",
		"if(`doc_mtto`.`creado`,date_format(`doc_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`doc_mtto`.`creado_por`" => "creado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`doc_mtto`.`id_doc`',
		2 => 2,
		3 => '`mantenimientos1`.`id_mtto`',
		4 => 4,
		5 => '`mantenimientos1`.`descripcion`',
		6 => 6,
		7 => 7,
		8 => '`biomedicos1`.`registro_invima`',
		9 => '`biomedicos1`.`firma`',
		10 => '`doc_mtto`.`creado`',
		11 => 11,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`doc_mtto`.`id_doc`" => "id_doc",
		"`doc_mtto`.`doc`" => "doc",
		"IF(    CHAR_LENGTH(`mantenimientos1`.`id_mtto`), CONCAT_WS('',   `mantenimientos1`.`id_mtto`), '') /* Id mtto */" => "id_mtto",
		"IF(    CHAR_LENGTH(`tipo_mtto1`.`tipo_mtto`), CONCAT_WS('',   `tipo_mtto1`.`tipo_mtto`), '') /* Tipo mtto */" => "tipo_mtto",
		"IF(    CHAR_LENGTH(`mantenimientos1`.`descripcion`), CONCAT_WS('',   `mantenimientos1`.`descripcion`), '') /* Mtto */" => "mtto",
		"`doc_mtto`.`observaciones`" => "observaciones",
		"IF(    CHAR_LENGTH(`biomedicos1`.`nombre`), CONCAT_WS('',   `biomedicos1`.`nombre`), '') /* Nombre profesional */" => "nombre_biomedico",
		"IF(    CHAR_LENGTH(`biomedicos1`.`registro_invima`), CONCAT_WS('',   `biomedicos1`.`registro_invima`), '') /* Invima */" => "registro_invima",
		"IF(    CHAR_LENGTH(`biomedicos1`.`firma`), CONCAT_WS('',   `biomedicos1`.`firma`), '') /* Firma profesional */" => "firma_biomedico",
		"if(`doc_mtto`.`creado`,date_format(`doc_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`doc_mtto`.`creado_por`" => "creado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`doc_mtto`.`id_doc`" => "ID",
		"`doc_mtto`.`doc`" => "Doc",
		"IF(    CHAR_LENGTH(`mantenimientos1`.`id_mtto`), CONCAT_WS('',   `mantenimientos1`.`id_mtto`), '') /* Id mtto */" => "Id mtto",
		"IF(    CHAR_LENGTH(`tipo_mtto1`.`tipo_mtto`), CONCAT_WS('',   `tipo_mtto1`.`tipo_mtto`), '') /* Tipo mtto */" => "Tipo mtto",
		"IF(    CHAR_LENGTH(`mantenimientos1`.`descripcion`), CONCAT_WS('',   `mantenimientos1`.`descripcion`), '') /* Mtto */" => "Mtto",
		"`doc_mtto`.`observaciones`" => "Observaciones",
		"IF(    CHAR_LENGTH(`biomedicos1`.`nombre`), CONCAT_WS('',   `biomedicos1`.`nombre`), '') /* Nombre profesional */" => "Nombre profesional",
		"IF(    CHAR_LENGTH(`biomedicos1`.`registro_invima`), CONCAT_WS('',   `biomedicos1`.`registro_invima`), '') /* Invima */" => "Invima",
		"IF(    CHAR_LENGTH(`biomedicos1`.`firma`), CONCAT_WS('',   `biomedicos1`.`firma`), '') /* Firma profesional */" => "Firma profesional",
		"`doc_mtto`.`creado`" => "Creado",
		"`doc_mtto`.`creado_por`" => "Creado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`doc_mtto`.`id_doc`" => "id_doc",
		"`doc_mtto`.`doc`" => "doc",
		"IF(    CHAR_LENGTH(`mantenimientos1`.`id_mtto`), CONCAT_WS('',   `mantenimientos1`.`id_mtto`), '') /* Id mtto */" => "id_mtto",
		"IF(    CHAR_LENGTH(`tipo_mtto1`.`tipo_mtto`), CONCAT_WS('',   `tipo_mtto1`.`tipo_mtto`), '') /* Tipo mtto */" => "tipo_mtto",
		"IF(    CHAR_LENGTH(`mantenimientos1`.`descripcion`), CONCAT_WS('',   `mantenimientos1`.`descripcion`), '') /* Mtto */" => "mtto",
		"`doc_mtto`.`observaciones`" => "observaciones",
		"IF(    CHAR_LENGTH(`biomedicos1`.`nombre`), CONCAT_WS('',   `biomedicos1`.`nombre`), '') /* Nombre profesional */" => "nombre_biomedico",
		"IF(    CHAR_LENGTH(`biomedicos1`.`registro_invima`), CONCAT_WS('',   `biomedicos1`.`registro_invima`), '') /* Invima */" => "registro_invima",
		"IF(    CHAR_LENGTH(`biomedicos1`.`firma`), CONCAT_WS('',   `biomedicos1`.`firma`), '') /* Firma profesional */" => "firma_biomedico",
		"if(`doc_mtto`.`creado`,date_format(`doc_mtto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`doc_mtto`.`creado_por`" => "creado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['id_mtto' => 'Id mtto', 'nombre_biomedico' => 'Nombre profesional', ];

	$x->QueryFrom = "`doc_mtto` LEFT JOIN `mantenimientos` as mantenimientos1 ON `mantenimientos1`.`id_mtto`=`doc_mtto`.`id_mtto` LEFT JOIN `biomedicos` as biomedicos1 ON `biomedicos1`.`id`=`doc_mtto`.`nombre_biomedico` LEFT JOIN `tipo_mtto` as tipo_mtto1 ON `tipo_mtto1`.`id`=`mantenimientos1`.`tipo_mtto` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'doc_mtto_view.php';
	$x->RedirectAfterInsert = 'doc_mtto_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Doc mtto';
	$x->TableIcon = 'resources/table_icons/attributes_display.png';
	$x->PrimaryKey = '`doc_mtto`.`id_doc`';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Doc', 'Tipo mtto', 'Mtto', 'Observaciones', 'Nombre profesional', 'Creado', ];
	$x->ColFieldName = ['doc', 'tipo_mtto', 'mtto', 'observaciones', 'nombre_biomedico', 'creado', ];
	$x->ColNumber  = [2, 4, 5, 6, 7, 10, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/doc_mtto_templateTV.html';
	$x->SelectedTemplate = 'templates/doc_mtto_templateTVS.html';
	$x->TemplateDV = 'templates/doc_mtto_templateDV.html';
	$x->TemplateDVP = 'templates/doc_mtto_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: doc_mtto_init
	$render = true;
	if(function_exists('doc_mtto_init')) {
		$args = [];
		$render = doc_mtto_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: doc_mtto_header
	$headerCode = '';
	if(function_exists('doc_mtto_header')) {
		$args = [];
		$headerCode = doc_mtto_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: doc_mtto_footer
	$footerCode = '';
	if(function_exists('doc_mtto_footer')) {
		$args = [];
		$footerCode = doc_mtto_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
