<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/foto_observacion.php");
	include_once("{$currDir}/foto_observacion_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('foto_observacion');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'foto_observacion';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`foto_observacion`.`id`" => "id",
		"IF(    CHAR_LENGTH(`observaciones1`.`id`), CONCAT_WS('',   `observaciones1`.`id`), '') /* Id observacion */" => "id_observacion",
		"`foto_observacion`.`foto`" => "foto",
		"`foto_observacion`.`descripcion`" => "descripcion",
		"if(`foto_observacion`.`creado`,date_format(`foto_observacion`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`foto_observacion`.`creado_por`" => "creado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`foto_observacion`.`id`',
		2 => '`observaciones1`.`id`',
		3 => 3,
		4 => 4,
		5 => '`foto_observacion`.`creado`',
		6 => 6,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`foto_observacion`.`id`" => "id",
		"IF(    CHAR_LENGTH(`observaciones1`.`id`), CONCAT_WS('',   `observaciones1`.`id`), '') /* Id observacion */" => "id_observacion",
		"`foto_observacion`.`foto`" => "foto",
		"`foto_observacion`.`descripcion`" => "descripcion",
		"if(`foto_observacion`.`creado`,date_format(`foto_observacion`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`foto_observacion`.`creado_por`" => "creado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`foto_observacion`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`observaciones1`.`id`), CONCAT_WS('',   `observaciones1`.`id`), '') /* Id observacion */" => "Id observacion",
		"`foto_observacion`.`descripcion`" => "Descripcion",
		"`foto_observacion`.`creado`" => "Creado",
		"`foto_observacion`.`creado_por`" => "Creado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`foto_observacion`.`id`" => "id",
		"IF(    CHAR_LENGTH(`observaciones1`.`id`), CONCAT_WS('',   `observaciones1`.`id`), '') /* Id observacion */" => "id_observacion",
		"`foto_observacion`.`descripcion`" => "descripcion",
		"if(`foto_observacion`.`creado`,date_format(`foto_observacion`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`foto_observacion`.`creado_por`" => "creado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['id_observacion' => 'Id observacion', ];

	$x->QueryFrom = "`foto_observacion` LEFT JOIN `observaciones` as observaciones1 ON `observaciones1`.`id`=`foto_observacion`.`id_observacion` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'foto_observacion_view.php';
	$x->RedirectAfterInsert = 'foto_observacion_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Fotos Observaciones';
	$x->TableIcon = 'resources/table_icons/pictures.png';
	$x->PrimaryKey = '`foto_observacion`.`id`';
	$x->DefaultSortField = '`foto_observacion`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, ];
	$x->ColCaption = ['Id observacion', 'Foto', 'Descripcion', ];
	$x->ColFieldName = ['id_observacion', 'foto', 'descripcion', ];
	$x->ColNumber  = [2, 3, 4, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/foto_observacion_templateTV.html';
	$x->SelectedTemplate = 'templates/foto_observacion_templateTVS.html';
	$x->TemplateDV = 'templates/foto_observacion_templateDV.html';
	$x->TemplateDVP = 'templates/foto_observacion_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: foto_observacion_init
	$render = true;
	if(function_exists('foto_observacion_init')) {
		$args = [];
		$render = foto_observacion_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: foto_observacion_header
	$headerCode = '';
	if(function_exists('foto_observacion_header')) {
		$args = [];
		$headerCode = foto_observacion_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: foto_observacion_footer
	$footerCode = '';
	if(function_exists('foto_observacion_footer')) {
		$args = [];
		$footerCode = foto_observacion_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
