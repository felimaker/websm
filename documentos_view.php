<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/documentos.php");
	include_once("{$currDir}/documentos_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('documentos');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'documentos';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`documentos`.`id`" => "id",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "dispo_codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "dispo_serial",
		"IF(    CHAR_LENGTH(`tipo_documento1`.`tipo_doc`), CONCAT_WS('',   `tipo_documento1`.`tipo_doc`), '') /* Tipo de documento */" => "tipo_docu",
		"`documentos`.`enlace`" => "enlace",
		"`documentos`.`descripcion`" => "descripcion",
		"`documentos`.`fecha_carga`" => "fecha_carga",
		"if(`documentos`.`creado`,date_format(`documentos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`documentos`.`creado_por`" => "creado_por",
		"if(`documentos`.`editado`,date_format(`documentos`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`documentos`.`editado_por`" => "editado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`documentos`.`id`',
		2 => '`dispositivos1`.`code`',
		3 => '`dispositivos1`.`serial`',
		4 => '`tipo_documento1`.`tipo_doc`',
		5 => 5,
		6 => 6,
		7 => 7,
		8 => '`documentos`.`creado`',
		9 => 9,
		10 => '`documentos`.`editado`',
		11 => 11,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`documentos`.`id`" => "id",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "dispo_codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "dispo_serial",
		"IF(    CHAR_LENGTH(`tipo_documento1`.`tipo_doc`), CONCAT_WS('',   `tipo_documento1`.`tipo_doc`), '') /* Tipo de documento */" => "tipo_docu",
		"`documentos`.`enlace`" => "enlace",
		"`documentos`.`descripcion`" => "descripcion",
		"`documentos`.`fecha_carga`" => "fecha_carga",
		"if(`documentos`.`creado`,date_format(`documentos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`documentos`.`creado_por`" => "creado_por",
		"if(`documentos`.`editado`,date_format(`documentos`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`documentos`.`editado_por`" => "editado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`documentos`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "Codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "Serial",
		"IF(    CHAR_LENGTH(`tipo_documento1`.`tipo_doc`), CONCAT_WS('',   `tipo_documento1`.`tipo_doc`), '') /* Tipo de documento */" => "Tipo de documento",
		"`documentos`.`enlace`" => "Enlace",
		"`documentos`.`descripcion`" => "Descripcion",
		"`documentos`.`fecha_carga`" => "Fecha de registro",
		"`documentos`.`creado`" => "Creado",
		"`documentos`.`creado_por`" => "Creado por",
		"`documentos`.`editado`" => "Editado",
		"`documentos`.`editado_por`" => "Editado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`documentos`.`id`" => "id",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Codigo */" => "dispo_codigo",
		"IF(    CHAR_LENGTH(`dispositivos1`.`serial`), CONCAT_WS('',   `dispositivos1`.`serial`), '') /* Serial */" => "dispo_serial",
		"IF(    CHAR_LENGTH(`tipo_documento1`.`tipo_doc`), CONCAT_WS('',   `tipo_documento1`.`tipo_doc`), '') /* Tipo de documento */" => "tipo_docu",
		"`documentos`.`enlace`" => "enlace",
		"`documentos`.`descripcion`" => "descripcion",
		"`documentos`.`fecha_carga`" => "fecha_carga",
		"if(`documentos`.`creado`,date_format(`documentos`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`documentos`.`creado_por`" => "creado_por",
		"if(`documentos`.`editado`,date_format(`documentos`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`documentos`.`editado_por`" => "editado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['dispo_codigo' => 'Codigo', 'tipo_docu' => 'Tipo de documento', ];

	$x->QueryFrom = "`documentos` LEFT JOIN `dispositivos` as dispositivos1 ON `dispositivos1`.`id_dispo`=`documentos`.`dispo_codigo` LEFT JOIN `tipo_documento` as tipo_documento1 ON `tipo_documento1`.`id_tipodoc`=`documentos`.`tipo_docu` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = (getLoggedAdmin() !== false);
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'documentos_view.php';
	$x->RedirectAfterInsert = 'documentos_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Documentos del dispositivo';
	$x->TableIcon = 'resources/table_icons/page_add.png';
	$x->PrimaryKey = '`documentos`.`id`';
	$x->DefaultSortField = '`documentos`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Codigo', 'Serial', 'Tipo de documento', 'Enlace', 'Descripcion', 'Fecha de registro', 'Creado', 'Editado', ];
	$x->ColFieldName = ['dispo_codigo', 'dispo_serial', 'tipo_docu', 'enlace', 'descripcion', 'fecha_carga', 'creado', 'editado', ];
	$x->ColNumber  = [2, 3, 4, 5, 6, 7, 8, 10, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/documentos_templateTV.html';
	$x->SelectedTemplate = 'templates/documentos_templateTVS.html';
	$x->TemplateDV = 'templates/documentos_templateDV.html';
	$x->TemplateDVP = 'templates/documentos_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: documentos_init
	$render = true;
	if(function_exists('documentos_init')) {
		$args = [];
		$render = documentos_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: documentos_header
	$headerCode = '';
	if(function_exists('documentos_header')) {
		$args = [];
		$headerCode = documentos_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: documentos_footer
	$footerCode = '';
	if(function_exists('documentos_footer')) {
		$args = [];
		$footerCode = documentos_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
