<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/acceso_remoto.php");
	include_once("{$currDir}/acceso_remoto_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('acceso_remoto');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'acceso_remoto';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`acceso_remoto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Unidad */" => "unidad",
		"IF(    CHAR_LENGTH(`ciudades1`.`ciudad`), CONCAT_WS('',   `ciudades1`.`ciudad`), '') /* Ciudad */" => "ciudad",
		"`acceso_remoto`.`servicio`" => "servicio",
		"`acceso_remoto`.`anydesk`" => "anydesk",
		"`acceso_remoto`.`acceso`" => "acceso",
		"`acceso_remoto`.`teamviewer`" => "teamviewer",
		"`acceso_remoto`.`acceso2`" => "acceso2",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Equipo relacionado */" => "codigo_equipo",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`) || CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marcas1`.`marca`, ' - ', `marca_modelo1`.`modelo`), '') /* Equipo */" => "equipo",
		"if(`acceso_remoto`.`ultima_verifica`,date_format(`acceso_remoto`.`ultima_verifica`,'%d/%m/%Y'),'')" => "ultima_verifica",
		"`acceso_remoto`.`comentario`" => "comentario",
		"if(`acceso_remoto`.`creado`,date_format(`acceso_remoto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`acceso_remoto`.`creado_por`" => "creado_por",
		"if(`acceso_remoto`.`editado`,date_format(`acceso_remoto`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`acceso_remoto`.`editado_por`" => "editado_por",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`acceso_remoto`.`id`',
		2 => '`unidades1`.`nombre`',
		3 => 3,
		4 => 4,
		5 => 5,
		6 => 6,
		7 => 7,
		8 => 8,
		9 => '`dispositivos1`.`code`',
		10 => 10,
		11 => '`acceso_remoto`.`ultima_verifica`',
		12 => 12,
		13 => '`acceso_remoto`.`creado`',
		14 => 14,
		15 => '`acceso_remoto`.`editado`',
		16 => 16,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`acceso_remoto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Unidad */" => "unidad",
		"IF(    CHAR_LENGTH(`ciudades1`.`ciudad`), CONCAT_WS('',   `ciudades1`.`ciudad`), '') /* Ciudad */" => "ciudad",
		"`acceso_remoto`.`servicio`" => "servicio",
		"`acceso_remoto`.`anydesk`" => "anydesk",
		"`acceso_remoto`.`acceso`" => "acceso",
		"`acceso_remoto`.`teamviewer`" => "teamviewer",
		"`acceso_remoto`.`acceso2`" => "acceso2",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Equipo relacionado */" => "codigo_equipo",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`) || CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marcas1`.`marca`, ' - ', `marca_modelo1`.`modelo`), '') /* Equipo */" => "equipo",
		"if(`acceso_remoto`.`ultima_verifica`,date_format(`acceso_remoto`.`ultima_verifica`,'%d/%m/%Y'),'')" => "ultima_verifica",
		"`acceso_remoto`.`comentario`" => "comentario",
		"if(`acceso_remoto`.`creado`,date_format(`acceso_remoto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`acceso_remoto`.`creado_por`" => "creado_por",
		"if(`acceso_remoto`.`editado`,date_format(`acceso_remoto`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`acceso_remoto`.`editado_por`" => "editado_por",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`acceso_remoto`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Unidad */" => "Unidad",
		"IF(    CHAR_LENGTH(`ciudades1`.`ciudad`), CONCAT_WS('',   `ciudades1`.`ciudad`), '') /* Ciudad */" => "Ciudad",
		"`acceso_remoto`.`servicio`" => "Servicio",
		"`acceso_remoto`.`anydesk`" => "Anydesk",
		"`acceso_remoto`.`acceso`" => "Acceso",
		"`acceso_remoto`.`teamviewer`" => "Teamviewer",
		"`acceso_remoto`.`acceso2`" => "Acceso Team",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Equipo relacionado */" => "Equipo relacionado",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`) || CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marcas1`.`marca`, ' - ', `marca_modelo1`.`modelo`), '') /* Equipo */" => "Equipo",
		"`acceso_remoto`.`ultima_verifica`" => "Ultima verificacion",
		"`acceso_remoto`.`comentario`" => "Comentario",
		"`acceso_remoto`.`creado`" => "Creado",
		"`acceso_remoto`.`creado_por`" => "Creado por",
		"`acceso_remoto`.`editado`" => "Editado",
		"`acceso_remoto`.`editado_por`" => "Editado por",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`acceso_remoto`.`id`" => "id",
		"IF(    CHAR_LENGTH(`unidades1`.`nombre`), CONCAT_WS('',   `unidades1`.`nombre`), '') /* Unidad */" => "unidad",
		"IF(    CHAR_LENGTH(`ciudades1`.`ciudad`), CONCAT_WS('',   `ciudades1`.`ciudad`), '') /* Ciudad */" => "ciudad",
		"`acceso_remoto`.`servicio`" => "servicio",
		"`acceso_remoto`.`anydesk`" => "anydesk",
		"`acceso_remoto`.`acceso`" => "acceso",
		"`acceso_remoto`.`teamviewer`" => "teamviewer",
		"`acceso_remoto`.`acceso2`" => "acceso2",
		"IF(    CHAR_LENGTH(`dispositivos1`.`code`), CONCAT_WS('',   `dispositivos1`.`code`), '') /* Equipo relacionado */" => "codigo_equipo",
		"IF(    CHAR_LENGTH(`marcas1`.`marca`) || CHAR_LENGTH(`marca_modelo1`.`modelo`), CONCAT_WS('',   `marcas1`.`marca`, ' - ', `marca_modelo1`.`modelo`), '') /* Equipo */" => "equipo",
		"if(`acceso_remoto`.`ultima_verifica`,date_format(`acceso_remoto`.`ultima_verifica`,'%d/%m/%Y'),'')" => "ultima_verifica",
		"`acceso_remoto`.`comentario`" => "comentario",
		"if(`acceso_remoto`.`creado`,date_format(`acceso_remoto`.`creado`,'%d/%m/%Y %h:%i %p'),'')" => "creado",
		"`acceso_remoto`.`creado_por`" => "creado_por",
		"if(`acceso_remoto`.`editado`,date_format(`acceso_remoto`.`editado`,'%d/%m/%Y %h:%i %p'),'')" => "editado",
		"`acceso_remoto`.`editado_por`" => "editado_por",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['unidad' => 'Unidad', 'codigo_equipo' => 'Equipo relacionado', ];

	$x->QueryFrom = "`acceso_remoto` LEFT JOIN `unidades` as unidades1 ON `unidades1`.`id_unidades`=`acceso_remoto`.`unidad` LEFT JOIN `dispositivos` as dispositivos1 ON `dispositivos1`.`id_dispo`=`acceso_remoto`.`codigo_equipo` LEFT JOIN `ciudades` as ciudades1 ON `ciudades1`.`id_ciudad`=`unidades1`.`ciudad` LEFT JOIN `marcas` as marcas1 ON `marcas1`.`id_marca`=`dispositivos1`.`marca` LEFT JOIN `marca_modelo` as marca_modelo1 ON `marca_modelo1`.`id_mmodelo`=`dispositivos1`.`modelo` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'acceso_remoto_view.php';
	$x->RedirectAfterInsert = 'acceso_remoto_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Acceso remoto';
	$x->TableIcon = 'resources/table_icons/dns.png';
	$x->PrimaryKey = '`acceso_remoto`.`id`';
	$x->DefaultSortField = '`acceso_remoto`.`creado`';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['Unidad', 'Ciudad', 'Servicio', 'Anydesk', 'Acceso', 'Equipo relacionado', 'Ultima verificacion', 'Editado por', ];
	$x->ColFieldName = ['unidad', 'ciudad', 'servicio', 'anydesk', 'acceso', 'codigo_equipo', 'ultima_verifica', 'editado_por', ];
	$x->ColNumber  = [2, 3, 4, 5, 6, 9, 11, 16, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/acceso_remoto_templateTV.html';
	$x->SelectedTemplate = 'templates/acceso_remoto_templateTVS.html';
	$x->TemplateDV = 'templates/acceso_remoto_templateDV.html';
	$x->TemplateDVP = 'templates/acceso_remoto_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: acceso_remoto_init
	$render = true;
	if(function_exists('acceso_remoto_init')) {
		$args = [];
		$render = acceso_remoto_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: acceso_remoto_header
	$headerCode = '';
	if(function_exists('acceso_remoto_header')) {
		$args = [];
		$headerCode = acceso_remoto_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: acceso_remoto_footer
	$footerCode = '';
	if(function_exists('acceso_remoto_footer')) {
		$args = [];
		$footerCode = acceso_remoto_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
