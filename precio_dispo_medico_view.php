<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/precio_dispo_medico.php");
	include_once("{$currDir}/precio_dispo_medico_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('precio_dispo_medico');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'precio_dispo_medico';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`precio_dispo_medico`.`id`" => "id",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`id`), CONCAT_WS('',   `compra_dispo1`.`id`), '') /* Dispo medico */" => "dispo_medico",
		"`precio_dispo_medico`.`precio`" => "precio",
		"`precio_dispo_medico`.`proveedor`" => "proveedor",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`precio_dispo_medico`.`id`',
		2 => '`compra_dispo1`.`id`',
		3 => 3,
		4 => 4,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`precio_dispo_medico`.`id`" => "id",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`id`), CONCAT_WS('',   `compra_dispo1`.`id`), '') /* Dispo medico */" => "dispo_medico",
		"`precio_dispo_medico`.`precio`" => "precio",
		"`precio_dispo_medico`.`proveedor`" => "proveedor",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`precio_dispo_medico`.`id`" => "ID",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`id`), CONCAT_WS('',   `compra_dispo1`.`id`), '') /* Dispo medico */" => "Dispo medico",
		"`precio_dispo_medico`.`precio`" => "Precio",
		"`precio_dispo_medico`.`proveedor`" => "Proveedor",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`precio_dispo_medico`.`id`" => "id",
		"IF(    CHAR_LENGTH(`compra_dispo1`.`id`), CONCAT_WS('',   `compra_dispo1`.`id`), '') /* Dispo medico */" => "dispo_medico",
		"`precio_dispo_medico`.`precio`" => "precio",
		"`precio_dispo_medico`.`proveedor`" => "proveedor",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['dispo_medico' => 'Dispo medico', ];

	$x->QueryFrom = "`precio_dispo_medico` LEFT JOIN `compra_dispo` as compra_dispo1 ON `compra_dispo1`.`id`=`precio_dispo_medico`.`dispo_medico` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'precio_dispo_medico_view.php';
	$x->RedirectAfterInsert = 'precio_dispo_medico_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Precio dispo medico';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`precio_dispo_medico`.`id`';

	$x->ColWidth = [150, 150, 150, ];
	$x->ColCaption = ['Dispo medico', 'Precio', 'Proveedor', ];
	$x->ColFieldName = ['dispo_medico', 'precio', 'proveedor', ];
	$x->ColNumber  = [2, 3, 4, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/precio_dispo_medico_templateTV.html';
	$x->SelectedTemplate = 'templates/precio_dispo_medico_templateTVS.html';
	$x->TemplateDV = 'templates/precio_dispo_medico_templateDV.html';
	$x->TemplateDVP = 'templates/precio_dispo_medico_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: precio_dispo_medico_init
	$render = true;
	if(function_exists('precio_dispo_medico_init')) {
		$args = [];
		$render = precio_dispo_medico_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: precio_dispo_medico_header
	$headerCode = '';
	if(function_exists('precio_dispo_medico_header')) {
		$args = [];
		$headerCode = precio_dispo_medico_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: precio_dispo_medico_footer
	$footerCode = '';
	if(function_exists('precio_dispo_medico_footer')) {
		$args = [];
		$footerCode = precio_dispo_medico_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
